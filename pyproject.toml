[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "strategy-evaluation"
version = "0.1.0"
description = "Reusable strategy evaluation metrics for investment framework outputs"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["strategy_evaluation*"]
