[project]
name = "token-strategy"
version = "0.1.0"
description = "Token budget and RAG context optimization helpers for LLM commercialization calls."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
